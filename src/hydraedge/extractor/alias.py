# src/hydraedge/extractor/alias.py

def resolve(alias_key: str) -> str:
    """
    Simple alias resolver stub.

    Given an alias key (e.g. from node['alias_key']), return the canonical form.
    For now this is a no-op; expand with real logic as needed.
    """
    return alias_key